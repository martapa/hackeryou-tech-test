import React from 'react';

import logo from '../assets/tiff_logo.svg';

const Header = () => {
  return (
    <header>
      <img src={logo} alt="Tiff Logo" />
    </header>
  );
};

export default Header;
